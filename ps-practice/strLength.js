const words = ["nap time", "per scholas", "my name is ricardo", "sushi"];
for (let i = 0; i < words.length; i++) {
  console.log(
    `Index ${i} of the word array is ${words[i].length} characters long.`
  );
}
